<h1>Playlists</h1>
<ul>
<%- @playlists.each do |playlist| %>
  <li>
    <%= playlist.name %>
    <%= link_to "Watch", playlist_playlist_watches_path(playlist.id), method: :post %>
  </li>
<%- end %>
</ul>
